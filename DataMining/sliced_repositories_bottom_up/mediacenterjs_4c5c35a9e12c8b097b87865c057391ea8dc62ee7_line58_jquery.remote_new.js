(function($){
	function _websocketHandler(o, $that){
		if(io){
			$.ajax({
				}).done(function(data){
                var socket = io.connect(data.localIP+':'+data.remotePort);
				});
		} else {
	}
})(jQuery);
